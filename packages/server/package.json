{
  "name": "@abg/server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env dev --env-file ../../.env",
    "deploy": "wrangler deploy",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "latest",
    "better-auth": "latest",
    "dotenv": "^16.x",
    "drizzle-orm": "latest",
    "hono": "^4.x",
    "multer": "^1.x",
    "resend": "latest",
    "zod": "^3.x",
    "tsx": "^4.x"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.x",
    "@types/multer": "^1.x",
    "@types/node": "^20.x",
    "drizzle-kit": "latest",
    "typescript": "^5.x",
    "wrangler": "^4.x"
  }
}
