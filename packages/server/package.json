{
  "name": "@abg/server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env dev --env-file ../../.env --port 8788",
    "deploy": "wrangler deploy",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "seed": "tsx src/seed.ts",
    "generate:coupons": "tsx src/generateCoupons.ts",
    "populate:product-details": "tsx src/populateProductDetails.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "latest",
    "better-auth": "latest",
    "dotenv": "^16.x",
    "drizzle-orm": "latest",
    "hono": "^4.x",
    "multer": "^1.x",
    "nanoid": "^5.x",
    "resend": "latest",
    "zod": "^3.x",
    "tsx": "^4.x"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.x",
    "@types/multer": "^1.x",
    "@types/node": "^20.x",
    "drizzle-kit": "latest",
    "typescript": "^5.x",
    "wrangler": "^4.x"
  }
}
